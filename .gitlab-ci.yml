stages:
  - deploy

deploy_via_ftp:
  stage: deploy
  script:
    - lftp -c "
        open -u $FTP_USER,$FTP_PASS ftp://$FTP_HOST;
        mirror -R ./ domains/mijurnal.com/public_html/api-pos;
      "
  only:
    - main